clc
close all
clear

a=-9.81;
function v = V_perSec(t,a)
    acceleration = a;
    v = sqrt(2*9.81*125) +acceleration*(t);
end
s = (2*9.81*125)/(2*(-a));
t = linspace(0,5,20);
g = a/9.81;
v = V_perSec(t,a);
hold on
plot(t,v);
xlabel('time(s)')
ylabel('velocity (m/s)')
ylim([0,60]);
